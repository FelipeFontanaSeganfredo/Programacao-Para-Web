// Podemos usar a crase para criar string com mais de uma linha
let livro = `Livro de 
React`;

let preco = 50.98;

let str = `Livro 'do' Jorge`;

console.log(str);

// Interpolação, parecido com o python

let mensagem = `O nome do livro é ${livro} e o preço é R${preco}`;

console.log(mensagem);
